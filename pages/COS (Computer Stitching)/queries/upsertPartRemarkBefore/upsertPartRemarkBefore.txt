INSERT INTO upper_part_process (session_id, part_id, cos_remark_before)
VALUES (
  {{this.params.sessionId}},
  '{{this.params.partId}}',
  NULLIF('{{this.params.remark}}', '')
)
ON CONFLICT (session_id, part_id)
DO UPDATE SET
  cos_remark_before = NULLIF('{{this.params.remark}}', ''),
  updated_at = NOW();