{
  "gitSyncId": "6a451c9013289e1331252fc8_b0177646-b9ed-4309-8c7e-d8de3eb19836",
  "id": "Configuration_getUserList",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "SELECT\n  u.id,\n  u.username,\n  u.display_name,\n  u.role::text AS role,\n  u.is_active,\n  COALESCE(bool_or(c.client = 'PRB'), false) AS has_prb,\n  COALESCE(bool_or(c.client = 'PBB'), false) AS has_pbb,\n  u.email,\n  (u.keycloak_sub IS NOT NULL) AS sso_linked,\n  u.last_accessed\nFROM app_users u\nLEFT JOIN app_user_clients c ON c.user_id = u.id\nGROUP BY u.id, u.username, u.display_name, u.role, u.is_active, u.email, u.keycloak_sub, u.last_accessed\nORDER BY u.username;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "Panarub",
      "isAutoGenerated": false,
      "name": "Panarub",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [],
    "name": "getUserList",
    "pageId": "Configuration",
    "runBehaviour": "AUTOMATIC",
    "userSetOnLoad": false
  }
}