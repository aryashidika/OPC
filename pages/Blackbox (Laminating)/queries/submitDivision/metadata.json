{
  "gitSyncId": "6a451c9013289e1331252fc8_80490db7-f6ce-44c0-84d9-a8795b7842f8",
  "id": "Blackbox (Laminating)_submitDivision",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "INSERT INTO upper_division_status (session_id, division, state, submitted_at, submitted_by)\nVALUES (\n  {{this.params.sessionId}},\n  '{{this.params.division}}'::division_enum,\n  '{{this.params.state}}'::division_state_enum,\n  CASE WHEN '{{this.params.state}}' = 'SUBMITTED' THEN NOW() ELSE NULL END,\n  CASE WHEN '{{this.params.state}}' = 'SUBMITTED' THEN '{{this.params.submittedBy}}' ELSE NULL END\n)\nON CONFLICT (session_id, division)\nDO UPDATE SET\n  state        = '{{this.params.state}}'::division_state_enum,\n  submitted_at = CASE WHEN '{{this.params.state}}' = 'SUBMITTED' THEN NOW() ELSE upper_division_status.submitted_at END,\n  submitted_by = CASE WHEN '{{this.params.state}}' = 'SUBMITTED' THEN '{{this.params.submittedBy}}' ELSE upper_division_status.submitted_by END;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": false
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "Panarub",
      "isAutoGenerated": false,
      "name": "Panarub",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "submitDivision",
    "pageId": "Blackbox (Laminating)",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": false
  }
}