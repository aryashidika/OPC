SELECT * FROM upper_part
WHERE session_id = {{appsmith.store.ingestActiveSessionId}}
ORDER BY 
  SPLIT_PART(part_id, '-', 1)::int,
  CASE WHEN part_id LIKE '%-%' THEN SPLIT_PART(part_id, '-', 2)::int ELSE 0 END;