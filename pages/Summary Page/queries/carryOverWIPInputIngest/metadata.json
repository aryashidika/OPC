{
  "gitSyncId": "6a451c9013289e1331252fc8_f74a3bb2-9f96-4f6a-b027-7aaa6e342899",
  "id": "Summary Page_carryOverWIPInputIngest",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "INSERT INTO upper_wip_input (wip_id, input_kind, input_ref)\nSELECT wip_id, input_kind, input_ref\nFROM (\n  SELECT\n    new_w.id AS wip_id,\n    wi.input_kind,\n    CASE\n      WHEN wi.input_kind = 'PART' THEN (\n        SELECT np.id\n        FROM upper_part np\n        JOIN upper_part op ON op.part_id = np.part_id\n        WHERE op.id = wi.input_ref\n        AND np.session_id = {{this.params.newSessionId}}\n        LIMIT 1\n      )\n      WHEN wi.input_kind = 'WIP' THEN (\n        SELECT new_w2.id\n        FROM upper_wip new_w2\n        JOIN upper_wip old_w2 ON old_w2.label = new_w2.label\n        WHERE old_w2.id = wi.input_ref\n        AND new_w2.session_id = {{this.params.newSessionId}}\n        LIMIT 1\n      )\n    END AS input_ref\n  FROM upper_wip_input wi\n  JOIN upper_wip old_w ON old_w.id = wi.wip_id\n  JOIN upper_wip new_w ON new_w.label = old_w.label\n    AND new_w.session_id = {{this.params.newSessionId}}\n  WHERE old_w.session_id = {{this.params.oldSessionId}}\n) sub\nWHERE input_ref IS NOT NULL;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "Panarub",
      "isAutoGenerated": false,
      "name": "Panarub",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "carryOverWIPInputIngest",
    "pageId": "Summary Page",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": false
  }
}