UPDATE pbb_concern
SET
  is_closed  = true,
  closed_at  = NOW(),
  closed_by  = '{{this.params.closedBy}}'
WHERE id = {{this.params.concernId}}
  AND target_division = 'BLACKBOX'
  AND is_closed = false;